MERN stack

COMMANDS:
    Build with no cache -
        docker-compose build --no-cache
    Start the services -
        docker-compose up
    List the services -
        docker-compose ps
    List the containers -
        docker ps
    Stop services -
        docker-compose stop